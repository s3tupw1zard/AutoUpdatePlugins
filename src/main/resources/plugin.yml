name: AutoUpdatePlugins
version: '${project.version}'
main: io.github.aplini.autoupdateplugins.AutoUpdatePlugins
api-version: '1.20'
prefix: AUP
author: ApliNi

commands:
  aup:
    description: Use plugin commands
    permission: aup.command

permissions:
  aup.command:
    description: Use plugin commands
    default: op
